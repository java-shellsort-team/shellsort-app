# 🧩 ShellSort App

## 📖 Описание проекта
Учебный консольный проект для демонстрации сортировки автомобилей методом **Шелла** с применением паттернов проектирования  
(Strategy, Builder, Chain of Responsibility, Adapter, Decorator, Proxy).

Приложение позволяет:
- загружать данные о машинах из разных источников (файл, консоль, случайная генерация);
- сортировать их по выбранному признаку (мощность, год, модель);
- выводить результаты и логировать некорректные записи.

---

## 👥 Команда 3
- **Михаил (тимлид)** — организация работы, ревью, доработка архитектуры и финальная интеграция
- **Евгений** — реализация `Car` (Builder), помощь с `App.java`
- **Артём** — `Validator`, `LineParser`, `ConsoleDataProvider`
- **Давид** — `DataProvider`, `FileDataProvider`, `RandomDataProvider`
- **Иван** — стратегии сортировки (по году, модели, мощности)
- **Дмитрий** — алгоритм сортировки Шелла (`ShellSort`)

---

## 📅 План на спринт
1. Реализовать модель `Car` и валидатор.
2. Разработать парсер строк и провайдеры данных.
3. Добавить сортировки и алгоритм ShellSort.
4. Объединить всё в `App.java`.
5. Провести ревью и финальную сборку проекта.

---

## ✅ Текущее состояние (на 12 ноября)
- Все ключевые классы реализованы и протестированы:
  - `Car` (Builder) — Евгений
  - `Validator`, `LineParser`, `ConsoleDataProvider` — Артём
  - `FileDataProvider`, `RandomDataProvider` — Давид
  - `ShellSort` и стратегии — Иван и Дмитрий
- Проведено командное ревью, исправлены импорты и структура `input`.
- Проект стабильно собирается и запускается через `Gradle`.
- В планах — завершить интеграцию `App.java` и оформить финальный релиз.

---

## 🚀 Следующий шаг
- Завершить реализацию и проверку `App.java` (меню, запуск сортировки, отображение результатов).
- Проверить корректную работу всех `DataProvider` (файл, консоль, случайные данные).
- Обновить документацию и провести демонстрацию версии **v1.0 (13 ноября)**.

---

## 🧾 Отчёт тимлида

**Команда 3**  
Работа завершена в срок. Все модули (`model`, `input`, `strategy`, `sort`) реализованы и объединены.  
Проведены ревью и исправления: добавлены импорты, доработан `LoadResult`, унифицирована структура `input`.  
Давид создал итоговый PR с исправлениями `FileDataProvider` и `RandomDataProvider`, который был смержен.  
Евгений готов помочь с интеграцией `App.java` и финальным тестом запуска.  
Команда готова к выпуску версии **v1.0**.

---

## ⚙️ Технологии
- **Java 17+**
- **Gradle**
- Паттерны проектирования (Builder, Strategy, Decorator, Adapter, Proxy)
- JUnit 5 (тестирование)

---

## 🏁 Запуск проекта
1. Сборка:
   ```bash
   ./gradlew build
   ```
2. Запуск:
   ```bash
   ./gradlew run
   ```
3. Для чтения из файла — убедитесь, что `CarList.txt` находится в `src/main/resources`.

---

## 🧾 Кодировка консоли

Приложение принудительно использует UTF-8 для вывода в консоль, чтобы избежать проблем с кириллицей на Windows:

- в `App.main()` потоки `System.out` и `System.err` перенастраиваются на `UTF-8`;
- на Linux и macOS это работает «из коробки» и не требует дополнительных настроек;
- если в консоли всё ещё отображаются «кракозябры», стоит убедиться, что терминал или встроенная консоль IDE настроены на UTF-8.

---

## 📂 Структура пакетов
```
src/main/java/team/shellsort/
 ├── input/            # Провайдеры данных и парсер
 ├── model/            # Класс Car и Builder
 ├── sort/             # Алгоритм ShellSort
 ├── strategy/         # Стратегии сортировки
 ├── utils/            # Утилиты FileLines, Texts
 └── App.java          # Точка входа
```
---

© Команда 3 — ноябрь 2025
